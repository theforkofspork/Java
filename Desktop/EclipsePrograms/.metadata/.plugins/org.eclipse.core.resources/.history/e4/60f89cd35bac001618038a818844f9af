package com.Timer;

import java.awt.Canvas;
import java.awt.Graphics;
import java.awt.Graphics2D;
import java.awt.event.KeyEvent;
import java.awt.event.KeyListener;
import java.util.ArrayList;


public class Timer extends Canvas implements KeyListener{

	private static final long serialVersionUID = 5786145012149474206L;
	final int frameWidth = 500, frameHeight = frameWidth/12*9;
	double currentTime = 0.0;
	
	public Timer(){
		new Window(frameWidth, frameHeight, "Frame", this);
		this.setFocusable(true);
		this.addKeyListener(this);
	}

	
	public void paint(Graphics g)
	{
		Graphics2D g2 = (Graphics2D)g;
		g.drawString(genScramble(), 100,100);
	}
	
	
	public void keyTyped(KeyEvent e) {} public void keyReleased(KeyEvent e) {}
	
	
	public void keyPressed(KeyEvent e) {
		if(e.getKeyCode() == KeyEvent.VK_SPACE) System.out.println(genScramble());
		
	}
	
	
	public static void genScramble()
	{
		StringBuilder SB = new StringBuilder();
		int previousFace = 0, scrambleRV = 0;
		double markRV = 0.0;
		
		for(int i = 0; i < (int)Math.random()*2+20; i++){
			
			while(scrambleRV == previousFace)
			{
				scrambleRV = (int)Math.random()*5+1;
			}

			switch(scrambleRV)
			{
			case 1: SB.append("F");
			break;
			case 2: SB.append("B");
			break;
			case 3: SB.append("R");
			break;
			case 4: SB.append("L");
			break;
			case 5: SB.append("U");
			break;
			case 6: SB.append("B");
			break;
			
			}
			
			markRV = Math.random();
			if(markRV <= 0.4) SB.append("");
			else if(markRV >= 0.41 && markRV <= 0.6) SB.append("'");
			else if(markRV >= 0.61 && markRV <= 1.0) SB.append("2");
			
			previousFace = scrambleRV;
		}
		System.out.println("HI");
		//return SB.toString();
	}
	
	public static void main(String[] args)
	{
		genScramble();
		new Timer();
	}


	

	
}
